# @format

server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3308/cardsdb
    username: root
    password: root
  jpa:
    show-sql: true
    sql:
      init:
        mode: always
  database-platform: org.hibernate.dialect.MySQL8Dialect
